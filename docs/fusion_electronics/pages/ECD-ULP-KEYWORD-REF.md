# Keywords

# Keywords

次のキーワードは、特別な目的のために予約されており、通常の識別子名として使用することはできません。

break
case
char
continue
default
do
else
enum
for
if
int
numeric
real
return
string
switch
void
while
また、組み込みおよびオブジェクト タイプの名前も予約されており、識別子名として使用することはできません。