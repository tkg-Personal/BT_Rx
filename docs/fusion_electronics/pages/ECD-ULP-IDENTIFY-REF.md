# 識別子

# 識別子

識別子は、ユーザ定義の定数、変数、または関数を導入するために使用される名前です。識別子は、一連の英字(a b c...、A B C...)、数字(1 2 3...)、およびアンダースコア(_)で構成されます。識別子の最初の文字は、英字またはアンダースコアでなければなりません。

識別子では大文字と小文字が区別されます。つまり、

int Number, number;

は、2 つの異なる整数変数を定義します。識別子の最大長は 100 文字で、すべての文字が意味を持ちます。